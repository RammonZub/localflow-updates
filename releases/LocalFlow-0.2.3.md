## [0.2.3] - 2026-05-09

- Added a proactive Sparkle background check at app launch for release builds.
- Added a top-left Settings update badge that remains available when a user
  postpones an update.
- Removed the Settings footer as the primary update action so updates are
  announced by the app instead of discovered manually.

